<ul>
  {{#each @model as |foo|}}
    <li>
      {{foo.id}}
      -
      {{foo.name}}

      <button type="button" {{on "click" (fn this.deleteFoo foo)}}>
        Delete
      </button>
    </li>
  {{/each}}
</ul>